# 2026-06-30 工作记录

## 小程序视觉全面重构（按设计稿 1:1 实现）

参考设计稿（含 4 屏：首页 / 实时评估 / 评估报告 / 我的），对微信小程序前端进行了完整的视觉重构。
设计稿风格：圆角更大、颜色更柔和（淡蓝/淡绿/淡紫/淡橙四色功能卡）、去除硬阴影、强调"卡片化"布局。

### 涉及文件（18 个）
**全局**
- `miniprogram/app.wxss` — 重新定义 CSS 变量：主色 `#4f8cff`、柔和色卡、圆角 16/20/28rpx、新阴影
- `miniprogram/app.json` — tabBar 文案改为 首页/评估/记录/我的；导航栏背景白色、标题黑色

**首页 dashboard**
- `pages/dashboard/dashboard.{wxml,wxss,js}` — Hero 渐变横幅 + 2x2 功能宫格（蓝/青/紫/橙） + 今日数据 4 项（训练次数/平均分/锻炼次数/完成时长） + 推荐训练（带"换一换"） + 快捷入口 4 项
- 顶部状态栏高度通过 `wx.getSystemInfoSync().statusBarHeight` 适配

**实时评估 training**
- `pages/training/training.wxml` — 顶部返回 + 标题 + 静音/切换按钮；摄像头 + LIVE 红点徽章 + 浮动反馈气泡；3 列指标卡（膝/髋/躯干角度）；3 列计数（动作计数 / 当前评分高亮 / 卡路里）；3 列控制（切换摄像头 / 大暂停圆形 / 结束评估）；底部 4 个动作切换 tab
- `pages/training/training.wxss` — 蓝色主色、橙色评分高亮（#ff8a5b）、底部动作卡渐变激活
- `pages/training/training.js` — 新增 `metrics:{knee,hip,torso}` 与 `calories` 字段；`handleWSMessage` 兼容 `knee_angle`/`hip_angle`/`torso_angle` 与 `metrics.*` 多种字段名；新增 `togglePause`、`switchExercise`（切换动作会结束当前训练）

**评估报告 result**
- `pages/result/result.{wxml,wxss,js}` — 蓝色渐变综合评分卡（带优秀/良好标签 + ★）+ SVG 雷达图（5 维度：完成度/稳定性/标准度/节奏控制/移动速度）+ 关键问题分析 3 条（主要问题/次要问题标签）+ 改进建议 3 条 + 分享/保存底部双按钮
- 新增 `ISSUE_LIBRARY` 与 `SUGGESTION_LIBRARY`，按 8 种动作分别配置

**个人中心 profile**
- `pages/profile/profile.{wxml,wxss,js}` — 顶部返回 + 标题 + ···；用户卡（头像 + 名字 + Lv.8 渐变等级 + 累计天数 28天）；本周训练趋势（用 `rich-text` 渲染 SVG 圆滑折线 + 渐变填充 + 数据点 + 数值标签 + 周一~今天 x 轴）；训练记录 4 条（缩略图 + 时长 + 已完成徽章）；底部 3 张荣誉卡（金黄色奖杯 + 橙色火焰 + 蓝色靶心）
- 新增 `buildTrendSVG()`（用 Cubic Bezier 生成圆滑曲线 + gradient fill）
- 新增 `calcStreak()`、`calcToImprove()` 计算连续打卡与待改进动作数

**其他页面**
- `pages/reports/reports.{wxml,wxss,js}` — 顶部居中标题 + 时间 Tab + 4 列概览网格 + 各运动柱状图 + 训练历史列表（与 profile 同款缩略图卡）
- `pages/exercises/list.{wxml,wxss}` — 顶部居中标题 + 圆角搜索栏 + 分类 Tab + 新版动作卡（左侧渐变色块 + 名称 + 标签组 + 右箭头）
- `pages/exercises/detail.{wxml,wxss,js}` — 圆角 hero 头部 + 圆角 section 卡片 + 错误项带圆形警告图标

### 验证
- 所有 7 个页面的 JS 通过 `node --check`
- 所有 8 个 JSON 通过 `JSON.parse` 验证
- 雷达图与折线图均用 `rich-text` 节点注入 SVG 字符串（小程序不直接支持内联 SVG）

### 已知约束
- 折线图与雷达图依赖 `rich-text` 节点，若后端数据全为 0 时显示空图
- 训练页的 `metrics` 数据需后端 WebSocket 在 `analysis` 消息中提供 `knee_angle` / `hip_angle` / `torso_angle` 字段；当前前端兼容多种字段名以防后端未升级
- tabBar 图标沿用现有 4 个 PNG，未替换
