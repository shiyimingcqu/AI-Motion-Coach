<!-- 动作详情页 -->
<view class="detail-page">
  <!-- 头部 -->
  <view class="detail-header" style="background: linear-gradient(135deg, {{exercise.accentColor}}, {{exercise.accentColor}}cc)">
    <text class="detail-icon">{{exercise.icon}}</text>
    <text class="detail-name">{{exercise.name}}</text>
    <view class="detail-tags">
      <text class="detail-tag">{{exercise.category}}</text>
      <text class="detail-tag">{{exercise.level}}</text>
      <text class="detail-tag">{{exercise.duration}}</text>
    </view>
  </view>

  <view class="detail-content">
    <!-- 描述 -->
    <view class="card" wx:if="{{exercise.description}}">
      <view class="card-title">动作简介</view>
      <text class="desc-text">{{exercise.description}}</text>
    </view>

    <!-- 检测模式 -->
    <view class="card" wx:if="{{exercise.modes.length > 0}}">
      <view class="card-title">支持模式</view>
      <view class="mode-list">
        <view class="mode-item" wx:for="{{exercise.modes}}" wx:key="index">
          <text>{{item}}</text>
        </view>
      </view>
    </view>

    <!-- 常见错误 -->
    <view class="card" wx:if="{{exercise.errors.length > 0}}">
      <view class="card-title">常见错误</view>
      <view class="error-list">
        <view class="error-item" wx:for="{{exercise.errors}}" wx:key="index">
          <text class="error-dot">•</text>
          <text>{{item}}</text>
        </view>
      </view>
    </view>

    <!-- 训练统计 -->
    <view class="card" wx:if="{{exerciseStats}}">
      <view class="card-title">我的训练</view>
      <view class="stats-row">
        <view class="stat-item">
          <text class="stat-num">{{exerciseStats.total || 0}}</text>
          <text class="stat-label">训练次数</text>
        </view>
        <view class="stat-item">
          <text class="stat-num">{{exerciseStats.avgScore || 0}}</text>
          <text class="stat-label">平均分</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 开始训练 -->
  <view class="detail-footer">
    <button class="btn btn-primary btn-lg" bindtap="startTraining">开始训练</button>
  </view>
</view>
